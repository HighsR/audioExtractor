# 🎧 audioExtractor

Extract audio from any video file — fast, simple, and cross-platform.  
Give it a video (e.g., `.mp4`, `.mov`, `.mkv`) and get a clean audio file (`.mp3`, `.wav`) in seconds.

> Perfect for saving soundtracks, podcasts, lectures, or voice-overs.

---

## ✨ Features

- 🗂️ Supports `.mp4`, `.mov`, `.mkv`, and more  
- 🎵 Output: `.mp3`, `.wav`, and other audio formats  
- ⚡ Fast, lossless extraction powered by **FFmpeg**  
- 🧰 CLI tool or importable Python module  
- 🖥️ Works on **Windows**, **macOS**, and **Linux**

---

## 📦 Requirements

- **Python 3.8+**
- **FFmpeg** installed & added to PATH  
  - 🪟 Windows → [Download FFmpeg](https://ffmpeg.org/download.html) and add `/bin` to PATH  
  - 🍎 macOS → `brew install ffmpeg`  
  - 🐧 Linux → `sudo apt install ffmpeg`

Install Python dependencies:

```bash
pip install moviepy
